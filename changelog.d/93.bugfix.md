Verify development TestPyPI PEP 740 attestations against the exact `testpypi.yml` publisher and artifact subjects, and isolate repeated registry verification by version so stale artifacts cannot enter install evidence.
