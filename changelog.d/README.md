# Changelog fragments

Every user-visible 0.1.x change needs one fragment named `<issue>.<type>.md`, where type is `feature`, `bugfix`, `security`, or `doc`. Use a descriptive local identifier when no issue exists.
