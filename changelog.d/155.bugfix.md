Remove provider-private reasoning, encrypted/native replay payloads, and request `tool_choice` fields before persisting the canonical OCR result or binding receipt v5. Private-only removal preserves a byte-equivalent review projection and approval eligibility, while attempts to launder the same fields through findings or warnings are publication-filtered. Public tool-call and reasoning-token counters remain unchanged.
