Expose semantically distinct all-OCR tool totals, verified MCP-server calls, reconciled count-only `summary`/`list`/`get` evidence actions, and closed provider-neutral token buckets without publishing arguments or provider payloads.
