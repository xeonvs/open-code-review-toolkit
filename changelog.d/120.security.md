`ocr-ci review --preserve-private-artifacts` can retain owner-only OCR session/context state for local diagnosis without creating a posting receipt; validated GitLab merge-request execution rejects the flag before OCR starts and keeps deterministic cleanup mandatory.
